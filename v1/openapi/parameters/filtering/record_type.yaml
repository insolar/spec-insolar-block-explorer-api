x-component: 'parameters/recordTypeParam'
name: type
in: query
description: record type in a query.
required: false
schema:
  type: string
  enum:
    - state
    - request
    - result
example: state