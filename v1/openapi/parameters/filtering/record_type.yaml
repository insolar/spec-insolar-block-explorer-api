x-component: 'parameters/recordTypeParam'
name: type
in: query
description: Record type for filtering records.
required: false
schema:
  type: string
  enum:
    - state
    - request
    - result
example: state