x-component: 'parameters/recordTypeParam'
name: type
in: query
description: Record type to filter the obtained records by.
required: false
schema:
  type: string
  enum:
    - state
    - request
    - result
example: state