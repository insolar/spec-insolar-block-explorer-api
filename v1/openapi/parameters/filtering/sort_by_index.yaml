x-component: 'parameters/sort_by_index'
in: query
name: sort_by
description: Sorting direction based on index.
required: false
schema:
  type: string
  default: "index_asc"
  enum:
    - index_desc
    - index_asc
example: index_asc