x-component: 'parameters/sort_by_index'
in: query
name: sort_by
description: Sorting direction based on `index`.
required: false
schema:
  type: string
  default: "+index"
  enum:
    - -index
    - +index
example: +index