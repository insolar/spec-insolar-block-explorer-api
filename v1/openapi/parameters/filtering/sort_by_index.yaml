x-component: 'parameters/sort_by_index'
in: query
name: sort_by
description: Index-based sorting direction for the result set.
required: false
schema:
  type: string
  default: "index_asc"
  enum:
    - index_desc
    - index_asc
example: index_asc