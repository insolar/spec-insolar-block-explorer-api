x-component: 'parameters/sort_by_pulse'
in: query
name: sort_by
description: Pulse number-based sorting direction for the result set.
required: false
schema:
  type: string
  default: "+pulse_number"
  enum:
    - -pulse_number
    - +pulse_number
example: +pulse_number