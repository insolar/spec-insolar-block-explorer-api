x-component: 'responses/400Response'
description: |
  Bad request. One or more of the query parameters failed validation or were not provided.
  Change parameters and repeat your request.
  Fields that failed validation will be detailed where possible.
content:
  application/json:
    schema:
      $ref: '../schemas/code-validation-error.yaml'