$schema: http://json-schema.org/draft-07/schema#
$id: responses/search-pulse.yaml
description: Pulse response for Search.
title: Pulse response.
properties:
  type:
    type: string
    description: Result type.
    enum:
      - pulse
    example: pulse
  meta:
    type: object
    description: Meta data.
    properties:
      pulse_number:
        type: integer
        description: Pulse Number.
        example: 1