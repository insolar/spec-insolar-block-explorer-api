x-component: 'responses/searchResponse'
description: OK
content:
  application/json:
    schema:
      oneOf:
        - $ref: '../schemas/search-pulse.yaml'
        - $ref: '../schemas/search-jet-drop.yaml'
        - $ref: '../schemas/search-lifeline.yaml'
        - $ref: '../schemas/search-request.yaml'
        - $ref: '../schemas/search-original-request.yaml'
        - $ref: '../schemas/search-state.yaml'