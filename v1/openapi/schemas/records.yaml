x-component: "schemas/records"
description: A paginated array of Records.
type: object
properties:
  total:
    type: integer
    format: int64
    description: Total results.
    example: 100
  result:
    type: array
    items:
      $ref: './record.yaml'