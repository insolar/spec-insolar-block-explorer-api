x-component: "schemas/records"
description: A paged array of records.
type: object
properties:
  total:
    type: integer
    format: int64
    description: Total results.
    example: 100
  result:
    type: array
    items:
      allOf:
        - $ref: './record.yaml'