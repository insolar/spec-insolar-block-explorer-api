x-component: 'schemas/pulses'
description: A paginated array of pulses.
type: object
properties:
  total:
    type: integer
    format: int64
    description: Number of entries in the array.
    example: 100
  result:
    type: array
    description: Array of entries. Size depends on the `limit` filtering parameter.
    items:
      $ref: './pulse.yaml'